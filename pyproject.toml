[project]
name = "light-cv"
version = "0.1.0"
description = "Light detection using OpenCV"
# readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opencv-python>=4.13.0.90",
    "streamlit>=1.53.1",
]
